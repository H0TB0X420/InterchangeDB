[package]
name = "idb-wal"
version = "0.1.0"
edition = "2021"

[dependencies]
idb-core = { path = "../idb-core" }
idb-storage = { path = "../idb-storage" }
parking_lot = "0.12"
crc32fast = "1.3"

[dev-dependencies]
tempfile = "3.10"

[lints.clippy]
all = "warn"
