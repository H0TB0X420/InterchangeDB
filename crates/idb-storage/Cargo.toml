[package]
name = "idb-storage"
version = "0.1.0"
edition = "2021"

[dependencies]
idb-core = { path = "../idb-core" }
parking_lot = "0.12"
lru = "0.12"
crc32fast = "1.3"
serde = { version = "1", features = ["derive"] }
bincode = "1"

[dev-dependencies]
tempfile = "3.10"

[lints.clippy]
all = "warn"
