[package]
name = "testkit"
version = "0.1.0"
edition = "2021"
description = "Shared test/bench support for interchangedb: config registry, conformance harnesses, workloads, fault injection."
publish = false

[dependencies]
interchangedb = { path = ".." }
tempfile = "3.10"
proptest = "1.0"

[lints.clippy]
all = "warn"
